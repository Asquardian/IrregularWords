#ifndef H_FUNCTIONS
#define H_FUNCTIONS
#include <cstring>
#include <iostream>

int Menu();
int All(int i);
int Random();
std::string Words(int Form);
void Checker(std::string &Output);
std::string Save(std::string Name);
void Continue();
void Progress();
void Now();
void Check();

#endif
